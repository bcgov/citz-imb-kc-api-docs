<header>
  <div class="title-header">
    <h1 class="page-title"><%= title %></h1>
    <p class="page-description"><%= description %></p>
  </div>

  <!-- Endpoint data -->
  <div id="endpoint-data" style="display: none;" data-endpoints="<%= JSON.stringify(endpoints) %>"></div>

  <!-- Authentication Button -->
  <button class="authentication-button" onclick="login()">
    <h3>Login with IDIR</h3>
    <svg xmlns="http://www.w3.org/2000/svg" fill="#fff" height="20" width="20" viewBox="0 0 8 8"><path d="m3 0v1h4v5h-4v1h5v-7zm1 2v1h-4v1h4v1l2-1.5z"/></svg>
  </button>
</header>
